/// App-wide constants shared across features.
library;

import 'package:uuid/uuid.dart';

/// Path to the bundled sample PDF asset.
const String kSamplePdfPath = 'assets/sample.pdf';

/// Shared UUID generator instance.
const Uuid uuid = Uuid();
